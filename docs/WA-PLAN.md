# خطة تغطية سيناريوهات واتساب — 2026-09-24

- **المصدر:** [WA-SCENARIOS.md](WA-SCENARIOS.md). رموز الثغرات (ح/م/ت) ورموز المراحل (ع/ر/ف/ك) منه.
- **كل مرحلة = أمر Claude Code واحد**، على `sim-harness` فقط. لا prod، ولا main، وجدولة prod تبقى `[]` (يُتحقق منها عبر Cloudflare API قبل النشر وبعده).
- **كل كتابة في Odoo** (tenant مشترك بين sim وprod) تسبقها نسخة rollback JSON وسكربت تراجع، ونقل غرض واحد في كل مرة مع تحقق بعده.
- **كل مرحلة كود:** اختبارات في `tests/`، و`tsc` نظيف، و`npm test` أخضر، ونشر على sim، و`/health` = ok، وتحديث [STATUS.md](STATUS.md). لا إرسال واتساب لأي رقم حقيقي في التحقق؛ التحقق عبر `sim_outbound` أو اختبارات الوحدة.
- **اللهجة (قرار مقفل):** رسائل العملاء والموردين عامية خفيفة ومهذبة، والرسائل الداخلية والمالية فصحى.

## ملخص

| المرحلة | ما تحل | قوالب جديدة | تنتظر اعتماد Meta | تحتاج Odoo | تحتاج قراراً |
|---|---|---|---|---|---|
| 1. موثوقية الإرسال ✅ 09-24 | ح1، ح6، م11، ت1، ت5، ت6 | 0 | لا | لا | لا |
| 2. سلامة الطلب والأزرار ✅ 09-24 (والإصلاح الكامل لـ ح3 ينتظر قالباً) | ح2، ح3، ح4، ح7، ح8، ح9، ت14 | 0 (+1 صار مطلوباً) | لا | قيمة x_purpose واحدة (نُفّذت) | حُسمت |
| 3. الوسائط والشكاوى ومتابعة الفريق | ح5، م6، م7، م12، م13، ت2، ت3، ت4، ت7، ت8 | 0 | لا | لا | نعم (س10) |
| 4. إنشاء القوالب الستة عند Meta | م1، م2، م4 (الإنشاء) | **6** | **نعم** | قيمة x_purpose واحدة | نعم (النصوص، وإذن POST إلى Meta) |
| 5. ربط القوالب الستة | م1، م2، م4 (الربط) | 0 | تنتظر المرحلة 4 | نقل 6 أغراض | نعم (س3) |
| 6. رحلة العميل بالقوالب الجاهزة | م3، م8، م10، م15، م16، م19 | 0 | لا | قيمة x_purpose + حقل إلغاء الاشتراك | نعم (س4، س9) |
| 7. الموردون والمستودع | م5، م9، ت10، ت11، ت13 | 0 | لا | قيمتا x_purpose + أتمتة سطر الطلب | نعم (س6، س7، س11) |
| 8. المالك ومعالجة الشكوى | م14، م17، ت9، ت12 | 0 (+1 اختياري) | اختياري | قيمة x_purpose + أتمتة الشكوى | نعم (س8) |
| — | م18 (الفاتورة بعد التحصيل) | — | — | — | قرار فقط (س5) |

**الإجمالي:** 6 قوالب جديدة مطلوبة، و**قالب سابع صار مطلوباً بعد المرحلة 2** (§ «قوالب طلبتها المرحلتان 1 و2»)، وثامن اختياري، وقالب سابع اختياري (العمولة) إن تقرر. تُربط 7 قوالب جاهزة وتُترك الباقية كما في § «القوالب الجاهزة» من WA-SCENARIOS.

**الترتيب:** 1 ثم 2 ثم 3 للثغرات الحرجة. ويمكن تنفيذ 4 مباشرة بعد 1 لبدء مهلة الاعتماد عند Meta، ثم 5 حين يُعتمد.

---

## حالة التنفيذ — 2026-09-24

المرحلتان 1 و2 منفّذتان على sim (نسخة `dd118e03`)، والتفاصيل في [STATUS.md](STATUS.md) § 20 وحالة كل ثغرة في [WA-SCENARIOS.md](WA-SCENARIOS.md) § 6. الفروق عن الخطة:
- **ح2:** حُسم س1 بالزرين: الطلب لا يُحفظ مسودة تلقائياً، بل يقرر العميل.
- **ح7:** متابعة «تم الشراء» في كرون 06:00 الموجود، ولا كرون جديد لها.
- **ح8:** القفل مفتاح KV يُقرأ بعد كتابته (الكتابة الأخيرة تفوز)، ومهلته 7 أيام لأزرار السجل، و26 ساعة للقائمة الثابتة، و90 ثانية لأزرار الكوتيشن التي يحرسها بعد ذلك فحص الحالة.
- **ح1:** الحد الأعلى للمتغير 900 حرف في `fetchMeta`، وقوائم المتصلين 600 حرف.
- **إضافة:** أزرار قوالب الفريق (نوع `button`) صارت تصل لمعالجها، وقائمة الشراء المقصوصة تصل كاملة بأزرارها عند أي رد من المستودع.

### قوالب طلبتها المرحلتان 1 و2 (تُنشأ في المرحلة 4، بإذن POST منفصل)

| # | الاسم المقترح | الفئة | النص المقترح | المتغيرات | الأزرار | يحل |
|---|---|---|---|---|---|---|
| 7 | `utak_order_confirm_remind_v1` | UTILITY | تذكير من يو تاك: طلبك رقم {{1}} لم يُؤكَّد بعد، ويُلغى تلقائياً الساعة {{2}}. اضغط «تأكيد الطلب» ليدخل طلبات اليوم. | {{1}} ‎#1234، {{2}} ‏9:00 مساءً | QUICK_REPLY: تأكيد الطلب (`confirm_order_<id>`) · إلغاء (`cancel_order_<id>`) | ح3 خارج النافذة: اليوم يخرج `utak_order_update` بلا زر، ورد العميل يعيد الزر |
| 8 (اختياري) | `utak_purchase_list_remind_v1` | UTILITY | تذكير من يو تاك: قائمة شراء يوم {{1}} لم يُضغط عليها «تم الشراء» بعد، وفيها {{2}} صنفاً. اضغط الزر بعد الشراء أو «مشكلة» لإبلاغنا. | {{1}} ‏25 سبتمبر 2026، {{2}} ‏12 | QUICK_REPLY: تم الشراء · مشكلة | ح7: اليوم يُعاد `utak_purchase_list_v2` وفي {{2}} كلمة «تذكير» |

**الربط بعد الاعتماد:** غرض جديد `customer_order_remind` للسابع، يستدعيه `sendCutoffReminders` ([team.ts:81](../src/team.ts)) خارج النافذة بدل `utak_order_update`، والحمولتان على الزرين 0 و1. والثامن غرض `purchase_list_remind` في `followUpUnconfirmedPurchaseLists` ([team.ts:280](../src/team.ts)).

---

## المرحلة 1 — موثوقية الإرسال (كود فقط)

**تحل:** ح1 (المتغيرات متعددة الأسطر)، وح6 (الفشل لا يصل للمالك)، وم11 (إذن التسليم قبل «بدء الدوام»)، وت1، وت5، وت6.

**قوالب:** لا جديد ولا ربط.

**الكود:**
1. **تنظيف مركزي للمتغيرات:** دالة `sanitizeTemplateParam` (تستبدل الأسطر والتبويب بـ « · »، وتضغط المسافات، وتقص بحد آمن) تُستخرج من `sanitizeOwnerAlertParam` في [templates.ts:215](../src/templates.ts)، وتُطبق على كل متغير داخل `sendTemplateByPurpose` في [templates.ts:94](../src/templates.ts) وداخل `sendTemplate` في [meta.ts:610](../src/meta.ts). القص يقع على حد عنصر لا في منتصف كلمة، ويضيف «و N أخرى».
2. **القوائم بسطر واحد:** قائمة الشراء ([team.ts:87-92](../src/team.ts))، ومسار السائق ([team.ts:198](../src/team.ts))، وملخص التحصيل ([invoice.ts:630](../src/invoice.ts))، والفاتورة الاحتياطية ([invoice.ts:312](../src/invoice.ts)). وأي رد نصي من المستودع بعد القائمة يعيد إرسال القائمة كاملة داخل الجلسة ([index.ts:1943](../src/index.ts)).
3. **تنبيه الفشل للمالك:** دالة واحدة `alertSendFailure` تُستدعى من الفشل المتزامن ([meta.ts:426](../src/meta.ts)) ومن حالة `failed` في webhook ([index.ts:1721](../src/index.ts)). تنبيه واحد لكل مستقبل ولكل غرض في اليوم (مفتاح KV)، ويحمل الغرض ورمز Meta وآخر 4 أرقام. لا تنبيه على فشل رسالة المالك نفسه (تسجيل فقط)، حتى لا يستدعي التنبيهُ نفسَه.
4. **إذن التسليم:** حين تنجح «بداية الدوام»، يُؤجل نص إذن التسليم ([team.ts:248](../src/team.ts) ← [delivery-note.ts:318](../src/delivery-note.ts)) إلى قائمة الانتظار نفسها التي تحمل المواقع (`pending_loc:`)، ويُفرغ عند ضغط الزر ([index.ts:1914](../src/index.ts)).
5. **ت1:** [router.ts:544](../src/router.ts): رد مهذب («هذا الزر من رسالة قديمة، اكتب لنا طلبك مباشرة») مع `logMessageAnalysis`.
6. **ت5، التاريخ الإنجليزي:** [invoice.ts:292](../src/invoice.ts) و[quotation.ts:542](../src/quotation.ts) إلى `ar-SA-u-ca-gregory-nu-latn` بصيغة «24 سبتمبر 2026» (أرقام لاتينية، وتقويم ميلادي).
7. **ت6، قص 900 حرف:** [suppliers.ts:121-127](../src/suppliers.ts): القص عند آخر «، » قبل الحد، وإضافة «وغيرها (N)».

**التحقق:** اختبار لكل قالب متعدد الأسطر أن المتغيرات المرسلة بلا `\n`؛ وإعادة `sendDailyCollectionSummary` على sim عبر `/sim/trigger` ثم قراءة `sim_outbound` (المتوقع: لا `#132018`).

**التقدير:** قوالب جديدة 0 · اعتماد Meta لا · Odoo لا · قرار لا.

---

## المرحلة 2 — سلامة الطلب والأزرار

**تحل:** ح2، ح3، ح4، ح7، ح8، ح9، ت14.

**قوالب جاهزة تُربط:** `utak_order_update` (UTILITY، متغيران) بغرض جديد `customer_order_update`.

**الكود:**
1. **ح2، بعد 21:00:** [router.ts:147-152](../src/router.ts): نص صادق بدل «طلبك يوصلك بكرة الصبح»: «استقبال الطلبات مقفل الآن، ويفتح الساعة 6 صباحاً. طلبك ما تسجّل، أرسله بعد الساعة 6 ويوصلك في اليوم اللي بعده». وتنبيه مالك مقيّد. وإن قرر براء الحفظ (س1): مسودة بتاريخ الغد، ورسالة تأكيد بعد 06:00.
2. **ح3، الإقفال:** كرون جديد 20:00 على sim فقط (`0 17 * * *` في `[env.sim.triggers]` بـ [wrangler.toml:129](../wrangler.toml)، و`cutoff_reminder` في `CRON_JOB` بـ [auto-send-guard.ts:34](../src/auto-send-guard.ts)، ومهمة في `/sim/trigger`). يمر على طلبات اليوم في `waiting_confirmation`، وفي `draft` إن كان فيها أصناف:
   - إن كان العميل داخل النافذة: أزرار الكوتيشن (`quotationButtons`) أو زر «جهّز الكوتيشن».
   - إن كان خارجها: `utak_order_update` بـ `[رقم الطلب، "لم يُؤكَّد بعد، ويُلغى تلقائياً الساعة 9:00 مساءً"]`، ومفتاح KV يجعل أي رد منه خلال ساعتين يعيد إرسال الأزرار.
   - في `closeUnconfirmedOrders` ([team.ts:40](../src/team.ts)) و`cancelStaleWaitingOrders` ([odoo.ts:1126](../src/odoo.ts)): إلغاء `draft` ذي الأصناف أيضاً (إن وافق براء، س2)، وإشعار كل عميل أُلغي طلبه بالطريقة نفسها.
3. **ح4، حارس الحالة:** [router.ts:557-574](../src/router.ts): قراءة `x_state` قبل الكتابة. «تأكيد» من `draft` و`waiting_confirmation` فقط وقبل الإقفال. «إلغاء» و«تعديل» من `draft` و`waiting_confirmation` و`confirmed` قبل قائمة 21:15 فقط. غير ذلك رد يوضح الحالة («طلبك في التجهيز، تواصل معنا للتعديل») وتنبيه مالك.
4. **ح7، قائمة الشراء:** معالج `purchase_issue_<id>` في [router.ts](../src/router.ts) على نمط `delivery_issue_` ([router.ts:520](../src/router.ts)): مفتاح انتظار، والنص التالي يصل للمالك. وفي كرون 06:00 (`openOrderingWindow`، [suppliers.ts:386](../src/suppliers.ts)): قائمة أمس ما زالت `sent` = تنبيه مالك وإعادة إرسال القالب للمستودع.
5. **ح8، القفل:** مفتاح KV `btn_lock:<buttonId>` بمهلة دقيقتين يُكتب قبل المعالجة في `collect_*` ([router.ts:462](../src/router.ts))، و`delivered_` ([router.ts:485](../src/router.ts))، و`purchase_done_` ([router.ts:469](../src/router.ts))، و`standing_confirm_`. وبعد إنشاء الدفعة في `recordCollection` ([invoice.ts:491](../src/invoice.ts)): عدّ دفعات الفاتورة، وإن زادت على المتبقي فتنبيه مالك (KV ليس ذرياً، والفحص بعد الإنشاء هو الشبكة الثانية).
6. **ح9:** [standing.ts:44](../src/standing.ts): رفض التأكيد إن كان `x_last_triggered` اليوم (بتوقيت الرياض) أو بعد الإقفال، مع رد واضح.
7. **ت14:** [router.ts:469](../src/router.ts): قائمة `done` مسبقاً = «القائمة مؤكدة من قبل» بلا تنبيه مالك.

**Odoo:** إضافة `customer_order_update` إلى اختيارات `x_whatsapp_template.x_purpose`، وضبطها على القالب #64، مع rollback.

**التحقق:** اختبارات حالات الانتقال كلها، وضغطتان متزامنتان، ومحاكاة 20:00 و21:00 على sim.

**التقدير:** قوالب جديدة 0 · اعتماد Meta لا · Odoo قيمة اختيار + نقل غرض · قرار نعم (س1، س2).

---

## المرحلة 3 — الوسائط والشكاوى ومتابعة الفريق

**تحل:** ح5، م6، م7، م12، م13، ت2، ت3، ت4، ت7، ت8.

**قوالب:** لا (كل الردود داخل الجلسة لأن المستقبل راسلنا للتو).

**الكود:**
1. **ح5 وم6، الوسائط:** قبل `continue` في [index.ts:1885](../src/index.ts):
   - **عميل:** رد «وصلتنا رسالتك الصوتية، براء بيراجعها ويرد عليك. ولو تكرمت اكتب الطلب نصاً عشان يتسجل مباشرة»، وتنبيه مالك مقيّد (مرة لكل عميل في الساعة). ورقم جديد يأخذ الترحيب أيضاً.
   - **مورد:** رد «وصلتنا، نراجعها ونرد عليك»، وتعليم سجل الطلب «رد بوسائط»، وتنبيه مالك.
   - **فريق:** تنبيه مالك.
2. **م7، أزرار المورد:** في فرع المورد ([index.ts:1954](../src/index.ts)) قبل `handleSupplierReply`: «تعديل الأسعار» ← «أرسل الأسعار المعدلة»؛ «توقف اليوم» ← تسجيل في سجل الطلب وتنبيه مالك؛ «شكراً» ← لا رد.
3. **م12، متابعة السائق:** في كرون 18:00: محطات اليوم بلا «تم التسليم»، ومفاتيح `pending_loc:` لم تُفرغ = تنبيه مالك واحد بقائمة.
4. **م13، الشكاوى:** ([complaint.ts](../src/complaint.ts)، [router.ts:422-431](../src/router.ts))
   - زرّا «عندي ملاحظة» و«تحتاج تحسين» يضعان مفتاح انتظار، فالنص التالي شكوى.
   - الكلمات الواسعة («ناقص»، «غالي»، «مشكلة») تمر على المصنِّف قبل إنشاء شكوى إن كان للعميل طلب مفتوح اليوم.
   - شكوى مفتوحة (`new`) للعميل نفسه اليوم تُضاف إليها الرسالة بدل شكوى جديدة.
   - كرون 18:00 يسرد الشكاوى المفتوحة للمالك. ونص «خلال ساعة» حسب س10.
5. **ت2:** [router.ts:425-431](../src/router.ts): تسجيل إجابة التقييم في `logMessageAnalysis`.
6. **ت3:** [index.ts:1943](../src/index.ts): نص الفريق الحر يصل للمالك (مقيّد) مع رد «وصلت لبراء».
7. **ت4:** مهلة `pending_issue` و`pending_neighborhood` ([router.ts:260](../src/router.ts)، [router.ts:350](../src/router.ts)، [router.ts:536](../src/router.ts)) من 30 دقيقة إلى 3 ساعات.
8. **ت7 وت8:** [index.ts:1988](../src/index.ts): لا رد «أهلاً» من المصنِّف في الرسالة نفسها التي أخذت الترحيب، وتنبيه مالك مرة واحدة عند إنشاء عميل جديد في كل البيئات ([index.ts:1851](../src/index.ts)).

**التقدير:** قوالب جديدة 0 · اعتماد Meta لا · Odoo لا · قرار نعم (س10).

---

## المرحلة 4 — إنشاء القوالب الستة عند Meta

**تحل (الإنشاء فقط):** م1، م2، م4. **تحتاج إذناً صريحاً من براء لطلبات POST إلى Meta** (هذه الدراسة كانت GET فقط).

| # | الاسم المقترح | الفئة | النص المقترح | المتغيرات (مثال للمراجعة) | الأزرار |
|---|---|---|---|---|---|
| 1 | `utak_supplier_ask_v2` | UTILITY | صباح الخير {{1}}، معك يو تاك. نحتاج أسعارك اليوم لهالأصناف: {{2}}. ياليت ترد بسعر كل صنف وتعبئته (مثال: طماطم كرتون 25 ريال)، والله يعطيك العافية. | {{1}} اسم المورد (أحمد حسان)، {{2}} الأصناف (طماطم، خيار، بطاطس) | — |
| 2 | `utak_owner_alert_v2` | UTILITY | تنبيه تشغيلي من نظام يو تاك: {{1}}. يُرجى مراجعة التفاصيل في النظام واتخاذ الإجراء اللازم. | {{1}} نص التنبيه (قائمة شراء اليوم لم تُؤكد) | — |
| 3 | `utak_shift_start_v2` | UTILITY | مرحباً {{1}}، مهامك في يو تاك لهذا اليوم جاهزة. اضغط «بدء الدوام» لاستلام التفاصيل والمواقع. | {{1}} اسم الموظف (عمر) | QUICK_REPLY: بدء الدوام |
| 4 | `utak_standing_remind_v2` | UTILITY | مساء الخير {{1}}، قائمتك الثابتة عند يو تاك جاهزة لطلب بكرة. نرسلها مثل ما هي، ولا تحب تعدّل عليها؟ | {{1}} اسم العميل (مطعم الوادي) | QUICK_REPLY: تمام أرسلوها · أبغى أعدّل |
| 5 | `utak_pay_remind_v3` | UTILITY | تذكير من يو تاك: عزيزنا {{1}}، يوجد على حسابكم مبلغ مستحق قدره {{2}} ريال. نأمل التكرم بسداده في أقرب وقت، ولأي استفسار يسعدنا ردكم على هذه الرسالة. | {{1}} اسم العميل، {{2}} المجموع (1250.00) | — |
| 6 | `utak_quotation_pdf_v1` | UTILITY، رأس DOCUMENT | مرحباً {{1}}، مرفق عرض السعر رقم {{2}} من يو تاك بتاريخ {{3}}، بإجمالي {{4}} ريال. الأسعار سارية حتى موعد إقفال الطلبات في يوم الإصدار، ولأي استفسار يسعدنا ردكم على هذه الرسالة. | {{1}} العميل، {{2}} UTAK-Q-20260924-001، {{3}} 24 سبتمبر 2026، {{4}} 480 | — |

**ملاحظات على النصوص:**
- لا نص يبدأ أو ينتهي بمتغير، وترتيب المتغيرات في النص يطابق ترتيب ما يرسله الكود.
- رقم 2 يختلف لفظياً عن `utak_owner_alert` الحالي، لأن Meta قد يرفض قالباً بمحتوى مطابق لقالب موجود.
- رقم 5 يطبق القرار المقفل: رسالة واحدة بالمجموع، واسم «يو تاك»، وبلا «بدون انقطاع». وهو فصحى لأنه رسالة مالية.
- الموردون والعملاء (1 و4) بعامية خفيفة. الداخلية والمالية (2 و3 و5 و6) بالفصحى.

**الخطوات:**
1. قراءة (GET) للتأكد من أن الأسماء غير مستخدمة.
2. إنشاء الستة (POST) مع أمثلة المتغيرات.
3. GET للحالة والفئة. **إن أعاد Meta تصنيف أي قالب إلى MARKETING فيتوقف الأمر ويُبلَّغ براء**، ولا يُربط.
4. في Odoo: إضافة `customer_quotation_pdf` إلى اختيارات x_purpose (مع rollback). لا نقل أغراض في هذه المرحلة.
5. مزامنة القوالب (`/admin/wa-template-sync`) لتظهر في `x_whatsapp_template` بغرض `other`.

**التقدير:** قوالب جديدة **6** · اعتماد Meta **نعم** · Odoo قيمة اختيار واحدة · قرار نعم (اعتماد النصوص، وإذن POST).

---

## المرحلة 5 — ربط القوالب الستة (بعد الاعتماد)

**تحل:** م1، م2، م4.

**لكل قالب:** نقل الغرض من القديم إلى الجديد بعد rollback، ثم التحقق بـ `pickTemplate` أن الغرض يجد الجديد وحده بعدد المتغيرات الصحيح، وتحديث عقد الأغراض في `scripts/wa-templates-20260924-purpose-contract.mjs` و`src/wa-template-labels.json`. المتغيرات تتبع اسم القالب (كما في `welcomeParams`)، فيبقى التراجع ممكناً دون نشر.

| الغرض | من ← إلى | تغيير الكود |
|---|---|---|
| `supplier_ask` | `utak_supplier_daily_ask` ← `utak_supplier_ask_v2` | [suppliers.ts:52](../src/suppliers.ts) يطلب متغيرين، و[suppliers.ts:129](../src/suppliers.ts) يرسل `[اسم المورد، القائمة]` (والقديم `[القائمة]`). حد القص يراعي طول الاسم |
| `owner_alert` | `utak_owner_alert` ← `utak_owner_alert_v2` | لا تغيير (متغير واحد) |
| `team_shift_start` | `utak_shift_start` ← `utak_shift_start_v2` | لا تغيير (الزر رقم 0 بالحمولة `shift_start`) |
| `customer_daily_remind` | `utak_v2_daily_remind` ← `utak_standing_remind_v2` | لا تغيير (الزران 0 و1 بالحمولتين نفسيهما) |
| `customer_pay_remind` | `utak_v2_pay_remind` ← `utak_pay_remind_v3` | المتغيرات نفسها. وفي [outreach.ts:59-88](../src/outreach.ts): سقف الوتيرة (مفتاح KV لكل عميل بمهلة N أيام، وحد أقصى للمرات، حسب س3)، وتحويل رد «حولت/دفعت» أو صورة الإيصال خلال 48 ساعة إلى تنبيه المحصّل والمالك |
| `customer_quotation_pdf` | (لا يوجد) ← `utak_quotation_pdf_v1` | لا تغيير: [quotation.ts:575](../src/quotation.ts) يرسل المتغيرات الأربعة بالترتيب نفسه، والتاريخ عربي منذ المرحلة 1 |

**التقدير:** قوالب جديدة 0 · تنتظر المرحلة 4 · Odoo نقل 6 أغراض · قرار نعم (س3).

---

## المرحلة 6 — رحلة العميل بالقوالب الجاهزة

**تحل:** م3، م8، م10، م15، م16، م19.

**قوالب جاهزة تُربط:** `utak_out_for_delivery` (الغرض `customer_delivery_incoming` مربوط أصلاً)، و`utak_payment_received` (غرض جديد `customer_payment_received`).

**الكود:**
1. **م8، في الطريق:** عند ضغط السائق «بدء الدوام» ([index.ts:1904](../src/index.ts))، لكل محطة في مساره اليوم: `utak_out_for_delivery` بـ `[رقم الطلب، اسم السائق]`.
2. **م10، تأكيد الدفع:** [invoice.ts:564](../src/invoice.ts) و[receipt.ts:358](../src/receipt.ts) إلى `utak_payment_received` بـ `[المبلغ، رقم الفاتورة]`، والنص الحالي احتياطي.
3. **م3، إعادة التنشيط:** [outreach.ts:94-119](../src/outreach.ts): شرط طلب واحد على الأقل في تاريخ العميل، وآخر طلب قبل 14 يوماً. واستثناء من ألغى الاشتراك. ورسالة «إيقاف» (س9) تضبط الحقل وترد بتأكيد.
4. **م19، التقييم:** [outreach.ts:29-54](../src/outreach.ts): مرة كل 7 أيام لكل عميل (مفتاح KV)، واستثناء من ألغى الاشتراك.
5. **م15، تعديل الطلب:** توسيع `extractOrderItems` في [claude.ts](../src/claude.ts) ليعيد عمليات (إضافة/حذف/تغيير كمية/استبدال)، وتطبيقها على أسطر الطلب في [router.ts:138](../src/router.ts). ونية `edit_order` ([router.ts:128](../src/router.ts)) تمر على المسار نفسه بدل «استخدم الأزرار». و«أبغى أعدّل» في القائمة الثابتة ([standing.ts:58](../src/standing.ts)) ينشئ مسودة من أصناف القائمة أولاً، ثم تُطبق التعديلات. هذا أكبر بند في الخطة.
6. **م16، القائمة الثابتة بلا رد:** حسب س4. إن تقرر تذكير ثانٍ فيُرسل من كرون 20:00 (المرحلة 2) للقوائم غير المفعّلة اليوم.

**Odoo:** قيمة `customer_payment_received` + ضبطها على #56، وحقل منطقي جديد `x_wa_marketing_optout` على `res.partner`، مع rollback.

**التقدير:** قوالب جديدة 0 · اعتماد Meta لا · Odoo قيمة اختيار + حقل · قرار نعم (س4، س9).

---

## المرحلة 7 — الموردون والمستودع

**تحل:** م5، م9، ت13، ومصير ت10 وت11 حسب القرار.

**قوالب جاهزة تُربط:** `utak_supplier_price_nudge` (غرض جديد `supplier_price_nudge`)، و`utak_item_shortage` (غرض جديد `customer_item_shortage`). وبشرط القرار: `utak_v2_loading` (ت10)، و`utak_po_confirmed` و`utak_po_changed` (ت11).

**الكود:**
1. **م5، تذكير المورد:** في كرون 05:00 ([index.ts:82](../src/index.ts))، بعد حساب الموثوقية: لكل سجل طلب أسعار الليلة بلا رد: `utak_supplier_price_nudge` بـ `[اسم المورد، "6:00 صباحاً"]`. لا كرون جديد.
2. **م9، الصنف الناقص:** نقطة جديدة `/odoo/hook/line-shortage` (بالتوكن نفسه) تستقبل أتمتة Odoo عند تحول `x_daily_order_line.x_status` إلى `unavailable` أو `substituted`. ترسل `utak_item_shortage` بـ `[الصنف، رقم الطلب، البديل من x_substitute_product_id أو «لا يوجد»]` وحمولتي `shortage_ok_<line>` و`shortage_no_<line>`. ومعالجان في [router.ts](../src/router.ts) يكتبان الحالة. ويمكن أن يغذيها زر «مشكلة» في قائمة الشراء (المرحلة 2).
3. **ت13:** [suppliers.ts:149-152](../src/suppliers.ts): فشل إرسال طلب الأسعار يُنبّه المالك «لم يصل الطلب إلى…»، ولا يُحسب «لم يرد» في تقرير 06:00.

**Odoo:** قيمتان في x_purpose + ضبطهما على #48 و#63، وإجراء آلي (server action + webhook) على `x_daily_order_line`، مع rollback.

**التقدير:** قوالب جديدة 0 · اعتماد Meta لا · Odoo قيمتان + أتمتة · قرار نعم (س6، س7، س11).

---

## المرحلة 8 — المالك ومعالجة الشكوى

**تحل:** م14، م17، ومصير ت9 وت12 حسب القرار.

**قوالب جاهزة تُربط:** `utak_complaint_resolved` (غرض جديد `customer_complaint_resolved`)، و`utak_v2_summary` (الغرض `owner_summary` مربوط أصلاً، ومسموح للمالك في [meta.ts](../src/meta.ts) `OWNER_ALLOWED_PURPOSES`).

**الكود:**
1. **م14:** نقطة `/odoo/hook/complaint-resolved` تستقبل أتمتة Odoo عند `x_complaint.x_status = resolved`، وترسل `[رقم الطلب أو الشكوى، x_resolution_note بعد التنظيف]`.
2. **م17، ملخص اليوم:** في نهاية كرون 21:15 ([team.ts:54](../src/team.ts)): `utak_v2_summary` بـ `[الطلبات المؤكدة، توصيلات اليوم، المحصّل اليوم]`.
3. **ت12 (اختياري):** تنبيه «مشكلة توصيل» ([index.ts:1930](../src/index.ts)) يذكر أن `utak_delivery_delay` متاح للإرسال اليدوي من Odoo.
4. **ت9 (بشرط القرار):** نموذج عمولة في Odoo، وقالب UTILITY سابع اختياري `utak_commission_v2` بدل `utak_v2_commission` التسويقي.

**Odoo:** قيمة `customer_complaint_resolved` + ضبطها على #54، وإجراء آلي على `x_complaint`، مع rollback.

**التقدير:** قوالب جديدة 0 (+1 اختياري) · اعتماد Meta فقط للاختياري · Odoo قيمة + أتمتة · قرار نعم (س8).

---

## الأسئلة المفتوحة لبراء

1. الطلب بعد 21:00: يُحفظ مسودةً للغد، أم يُرفض بنص صادق فقط؟
2. تذكير الإقفال 20:00 مناسب؟ ويُلغى `draft` ذو الأصناف مع `waiting_confirmation` الساعة 21:00؟
3. تذكير الدفع: كل كم يوماً، وكم مرة بحد أقصى؟
4. القائمة الثابتة بلا رد: تُلغى لليوم أم تُرسل كما هي؟ وهل يُرسل تذكير ثانٍ الساعة 20:00؟
5. الفاتورة بعد التحصيل الكامل فقط: تبقى بعد 10-01، أم تُرسل وقت التسليم؟ (يحتاج رأي المحاسب)
6. أمر شراء مسبق للمورد: موجود، أم يبقى الشراء من السوق؟
7. «انتهاء التحميل»: خطوة فعلية أم تُهمل؟
8. العمولة: من يستحقها وعلى أي أساس؟
9. كلمة إيقاف الرسائل التسويقية: «إيقاف» وحدها أم معها «stop»؟
10. رد الشكوى: «خلال ساعة» أم «خلال اليوم»؟
11. من يعلّم الصنف ناقصاً في Odoo: المستودع أم براء؟
12. نصوص القوالب الستة في المرحلة 4: معتمدة كما هي؟ وإذن POST إلى Meta لإنشائها؟
