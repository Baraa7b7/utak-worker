{
  "name": "utak-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --experimental-loader=./tests/loader.mjs tests/sim-isolation.test.mts && node --experimental-strip-types --experimental-loader=./tests/loader.mjs tests/wa-inbox.test.mts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
